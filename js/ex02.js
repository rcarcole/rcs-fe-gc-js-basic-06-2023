// Declarar una variable llamada "radio" y asignarle un valor numérico que represente el radio
// de un círculo. Calcular el área del círculo (usando la fórmula π * radio^2) y mostrar el
// resultado en la consola.


let radio = 5;
let resultado = Math.PI * (radio * radio);

console.log('El area del circulo es: '+resultado);